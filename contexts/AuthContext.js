// Import the createContext function from React
import { createContext } from "react";

// Create an AuthContext using React's createContext function.
// This will be used for managing authentication-related state in the application.
export const AuthContext = createContext();